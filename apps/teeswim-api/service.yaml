apiVersion: v1
kind: Service
metadata:
  name: teeswim-api
  namespace: default
spec:
  type: NodePort
  selector:
    app: teeswim-api
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30401
