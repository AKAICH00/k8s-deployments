apiVersion: v1
kind: Service
metadata:
  name: teeswim-mockup
  namespace: default
spec:
  type: NodePort
  selector:
    app: teeswim-mockup
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30402
