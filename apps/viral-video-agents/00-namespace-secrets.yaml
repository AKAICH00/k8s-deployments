apiVersion: v1
kind: Namespace
metadata:
  name: viral-video-agents
---
apiVersion: v1
kind: Secret
metadata:
  name: db-secrets
  namespace: viral-video-agents
type: Opaque
stringData:
  # In production, use a sealed secret or external secrets manager
  url: "postgresql://postgres:password@postgres-service:5432/viral_video_db"
  password: "password"
---
apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: viral-video-agents
type: Opaque
stringData:
  # ============================================================
  # AI Model APIs
  # ============================================================
  google_api_key: "AIzaSyAk..."   # Gemini API for script generation
  kie_api_key: "fk_..."           # Kie/Fal API for video generation

  # ============================================================
  # Storage (Cloudflare R2)
  # ============================================================
  r2_audit_access_key: "..."
  r2_audit_secret_key: "..."

  # ============================================================
  # Audio/Speech Services
  # ============================================================
  elevenlabs_api_key: "..."       # ElevenLabs TTS
  sync_labs_api_key: "..."        # Lip sync service

  # ============================================================
  # Observability
  # ============================================================
  # OLD: LangSmith (deprecated in V2)
  langsmith_api_key: "ls_..."     # Keep for OLD agents

  # NEW: Langfuse (V2 observability)
  langfuse_secret_key: "..."      # Optional, for V2 tracing
  langfuse_public_key: "..."      # Optional, for V2 tracing
